.venv/
__pycache__/
*.pyc
.pytest_cache/
data/
*.db
.env
*.pem
keys/
# NOTE: the image builds the dashboard from frontend/ (multi-stage), so the
# source must stay in context. Only exclude its heavy build artifacts.
frontend/node_modules/
frontend/dist/
contracts/
tests/
.git/
*.md
scripts/
